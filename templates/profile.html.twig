{% extends "base.html.twig" %}
{% block title %}Blog | Profil{% endblock %}
{% block content %}
    <section id="profilSection">
        <h1>Mon Profil</h1>
        {% include 'messages.html.twig' %}
        <div>
            <h2>{{ app.session.get('pseudo') }}</h2><br/>
            <a href="../public/index.php?route=updatePassword" class="btn btn-success">Modifier mon mot de passe</a>
            {% if app.session.get('role') != 'admin'  %}
                <a href="../public/index.php?route=deleteAccount" class="btn btn-danger">Supprimer mon compte</a>
            {% endif %}
        </div>
        <br/>
        <a href="../public/index.php?route=homeBlog" class="btn btn-secondary">Retour à l'accueil</a>
    </section>
{% endblock %}